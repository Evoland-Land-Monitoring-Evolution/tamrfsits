# @package _global_

defaults:
  - override /training_module: spatiotemporal_fusion.yaml
  - override /datamodule: ls2s2_all.yaml

training_module:
  training_module:
    cca_loss: null

label: "ablation_lpips"
